apiVersion: v1
kind: Service
metadata:
  name: auth-svc
  namespace: inference
  labels:
    app.kubernetes.io/name: auth-service
    app.kubernetes.io/managed-by: auth-service
    app.kubernetes.io/component: auth
    app.kubernetes.io/part-of: auth-svc
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: auth-service
    app.kubernetes.io/component: auth
  ports:
  - name: http
    port: 80
    targetPort: 8000
    protocol: TCP
